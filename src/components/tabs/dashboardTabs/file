<div className="tabs-content">
        {activeTab !== null && (
          <ViewOrderTab title={tabs[activeTab].title} content={tabs[activeTab].content} />
        )}
      </div>





      .tabs-content{
        background-color:red;
        position: absolute;
        right: 0;
        top: -20vh;
        height: 95vh;
        width: 75vw;
        z-index: 1000;
      }